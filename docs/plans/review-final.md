# Required fixes

1. `src/strands/cli.py:109-115, 152-161, 326-344` — The CLI's documented runtime-error boundary is incomplete. `infer()` can raise `SegmentError`, output writes can raise `OSError`, and other expected domain failures are not converted to a diagnostic plus exit code 1. A TSV row `Bad\tQ` makes each of `strands run`, `gallery`, and `lint` print a Python traceback; the segment diagnostic also omits the row's orthography, contrary to spec §2's requirement to name the word and offending substring. Add failing subprocess/direct-`main` tests for unknown input IPA and an unwritable `--out`, then wrap and annotate these expected failures so the commands return 1 without a traceback.

2. `src/strands/cli.py:210-212` — `strands explain WORD --strand welsh --construction all` raises `ValueError: too many values to unpack` before the intended single-construction validation can run. This is a usage error under the declared interface and must return 2. Add a failing test for `--construction all`, validate the returned list before destructuring, and keep `explain` restricted to one construction.

3. `tests/test_properties.py:13-15, 33-39` — The Task 28 property suite checks only `DESC`, so it does not enforce spec §8's “no `UNREPAIRED` on the 144-word set” property over the engine outputs exposed by `run --construction all` and the gallery. The committed snapshot already contains seven unallowed Dutch failures: `sneachta` (`VOC`, `PATRO_NI`), `sméara` (`VOC`, `PATRO_NI`), and `shnámh` (`VOC`, `PATRO_O`, `PATRO_NI`), while `tests/allow-unrepaired.txt` is explicitly empty. Parameterize the determinism, inventory, stress, and unrepaired checks over every CLI-reachable construction that can be built from one entry, and fix the Dutch construction/phonotactic rules (or explicitly approve and record exceptions) before treating milestone 7 as complete.

4. `tests/test_rules_dutch.py:241-271` — Two strict `xfail`s hide direct failures of Task 26's acceptance criteria: Mode C is 0.7714 versus the required 0.80, and Mode E is 0.1538 versus the required 0.25. Consequently `uv run pytest -q` reports green only as `717 passed, 4 xfailed`, while the plan says milestone 7 requires a green suite and explicitly sets those bars. Do not use `xfail` to waive milestone acceptance: either fix the rule/harness behavior to meet the approved bars, or revise and approve the plan's thresholds before replacing the xfails with ordinary passing assertions.

# Suggestions

- `tests/test_cli.py:75` — Parenthesize and strengthen the gallery assertion. As written, operator precedence reduces it to passing whenever the substring `welsh` appears, so it does not actually require either `Seán` or the expected table header.
- Verification: `uv run pytest -q` completed with 717 passed and 4 xfailed in 28.48s. `uv run strands run sources/irish/test-words.tsv --strand all --construction all` exited 0 and emitted 5,760 data rows. The gallery was regenerated byte-identically to the snapshot; 20+ rows were read for sanity, with the seven unallowed `UNREPAIRED` cells above being the material issue found.
